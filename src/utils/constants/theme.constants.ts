import { ColorScheme } from "../types/theme.types";

export const htmlClassList = document.documentElement.classList;

// ! if adding some color scheme here, then add the same to index.html also
export const colorSchemes: ColorScheme[] = ['dark', 'light'];
